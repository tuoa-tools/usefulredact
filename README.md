# UsefulRedact

Checks whether the redaction in a document actually holds, entirely on your machine.

Work in progress: the README is written once the evaluation and the app are in.
See `PROJECT_DOCS/BRIEF.md` for the specification.
